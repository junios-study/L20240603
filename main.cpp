#include <iostream>

using namespace std;

inline int Sum(int A, int B)
{
	return A + B;
}

int main()
{
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);
	1 + 2;// Sum(1, 2);



	return 0;
}
